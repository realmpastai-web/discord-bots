version: '3.8'

services:
  moderation-bot:
    build: .
    container_name: quantmod-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/moderation.db